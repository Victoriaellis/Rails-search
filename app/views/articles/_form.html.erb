<div class="container">
<%= simple_form_for [article] do |f| %>
    <%= f.input :title, required: true, placeholder: "What is the article title?"%>
    <%= f.input :url, required: true, placeholder: "What is the article url?" %>
    <%= f.button :submit, class: "btn btn-primary" %>
  <% end %>
</div>
